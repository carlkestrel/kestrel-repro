"""Marker package — see ``scripts/commands/__init__.py`` for the
rationale. This package ships ``rules/*.md`` and ``rules/*.mdc`` as
package data so the Cursor plugin's rule files travel with the
wheel.
"""
__all__: list[str] = []
