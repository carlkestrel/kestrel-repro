"""Marker package — see ``scripts/commands/__init__.py`` for the
rationale. This package ships ``templates/**`` as package data so
that templates are locatable via importlib.resources from any cwd
after install.
"""
__all__: list[str] = []
