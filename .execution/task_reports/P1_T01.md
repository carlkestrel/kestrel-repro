# Task Report: P1_T01

## Status: PASS

**Phase**: P1_mode_contract (模式、研究合同和状态机)  
**Title**: 修改 reproctl.py get_default_state() 新增 project_mode 字段  
**Executed**: 2026-07-15T21:51:00+08:00  
**Duration**: ~3 分钟  

## Goal

state.json 包含 project_mode='reproduce' 和 mode_switches=[]；这是 Phase 1 的基础任务。

## Acceptance Tests

| Test | Result |
|---|---|
| `project_mode_in_state`: get_default_state()['project_mode'] == 'reproduce' | PASS |
| `mode_switches_in_state`: get_default_state()['mode_switches'] == [] | PASS |

## What Was Done

### Code Changes (scripts/reproctl.py)

1. **get_default_state() L104-L177**: 在 `"current_mode": "strict_repro"` 之后插入三个新字段：
   - `project_mode: "reproduce"` (默认值)
   - `mode_switches: []` (模式切换历史)
   - `control_flags: { AUTO_PROCEED, HUMAN_CHECKPOINT, EXTERNAL_REVIEW, REVIEW_DIFFICULTY, COMPUTE_BUDGET_GPU_HOURS, STORAGE_BUDGET_GB }`

2. **新增模块级常量**: `VALID_PROJECT_MODES = ('reproduce', 'diagnose', 'extend')`

3. **新增 4 个函数** (L182-L256):
   - `get_project_mode(state=None)` — 读取当前模式，向后兼容
   - `set_project_mode(mode, reason="")` — 切换模式 + 写入 audit trail + DECISION_LOG.md
   - `require_mode(required)` — 错误模式时 exit(1)
   - `_append_decision_log()` — 私有助手，最佳努力写入 DECISION_LOG.md

### Local Test Results (8/8 PASS)

```
[OK] test 1: default state has new fields
[OK] test 2: get_project_mode(None) safe fallback
[OK] test 3: VALID_PROJECT_MODES correct
[OK] test 4: set_project_mode appends audit trail + DECISION_LOG
[OK] test 5: ValueError: Invalid project_mode: 'invalid_mode'
[OK] test 6: require_mode(ext) blocks with exit 1
[OK] test 7: require_mode(reproduce) passes
[OK] test 8: backward compat — phase/gates/current_mode/repositories intact
```

### Backward Compatibility Verified

- `state['phase'] == 'init'` ✓
- `'gate_0_paper_audit' in state['gates']` ✓
- `state['current_mode'] == 'strict_repro'` ✓
- `state['repositories']['references'] == []` ✓
- 新字段仅追加，未删除任何旧字段

## Risks & Mitigations

| 风险 | 缓解 |
|---|---|
| Schema 变更破坏现有 state.json | 已用 `state.get("project_mode", "reproduce")` 提供默认值，旧 state.json 自动适配 |
| _append_decision_log 写盘失败 | 用 try/except 包裹，仅警告，不抛错 |
| set_project_mode 在错误的 cwd 调用 | save_state 用 get_state_path() 自动管理路径 |

## Next Steps

P1_T02 (新增 mode 函数) 已被本任务顺手完成（get/set/require_mode 在 P1_T01 一并实现）。

下一个 READY 任务：**P1_T03** (新建 templates/decision_log.md 模板)，无依赖。