# P3_T01..T03 — Human Checkpoint Hardening

**Phase**: P3_human_checkpoint
**Status**: PASS

## P3_T01 — `templates/human_checkpoints.md`
- File created (12 numbered items, each with description + what-to-do-instead)
- Acceptance: 12 items present + keyword coverage for all 12 categories (synth-data, split, metric, OOM, loss, batch, AMP, ckpt, cherry-pick, extend, budget, unverifiable)

## P3_T02 — `reproctl.py human-checkpoint`
- Function `human_checkpoint(action, reason, approved_by, item)` added
- CLI sub-command `human-checkpoint --action {check,override}` registered
- 5/5 acceptance tests PASS:
  - A: flag=true (default) → check returns exit 1 + prints 12 items
  - B: flag=false → check returns exit 0
  - C: override without `--approved-by` → exit 2
  - D: override with `--approved-by alice` → exit 0 + decision row in DECISION_LOG.md
  - E: unknown action → exit 2
- Decision log row written with `decision_type=risk_override`, `actor=human`

## P3_T03 — `commands/repro-launch.md` pre-launch checks
- Appended "Pre-launch Checks (P3 hardening)" section
- 3 checks documented: human-checkpoint, compute budget, mode (not `extend`)
- Abort sequence + audit row writing specified

## Files
- `templates/human_checkpoints.md` (new)
- `scripts/reproctl.py` (+~120 lines)
- `commands/repro-launch.md` (+~55 lines)

## Evidence
- `evidence/P3_T02_human_checkpoint_test.log`