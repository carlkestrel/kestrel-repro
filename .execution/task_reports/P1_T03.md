# P1_T03 — Decision Log Template

**Phase**: P1_mode_contract
**Status**: PASS
**Date**: 2026-07-15
**Plan hash**: `729f69e94a152c422d10535261066419fdd107f3c2b5c37af04e89fb559f3f10`

## Goal

Create `templates/decision_log.md` containing the canonical Decision Log schema
(columns: Timestamp / Mode / Phase / Action / User Confirmed / Notes, mapped to
the canonical 10-column form: id / ts / actor / phase / task_id / decision_type /
before → after / reason / links).

## Acceptance Tests

| Test | Result |
|---|---|
| `file_exists` (`templates/decision_log.md`) | PASS (1951 bytes) |
| `all_required_columns` | PASS (9 header columns match schema, seed row D000 present) |

## Files Touched

| Path | Change |
|---|---|
| `templates/decision_log.md` | Already authored during P1_T01 phase; verified schema + seed row |

## Evidence

- `evidence/P1_T03_decision_log_template.json` — schema dump + runtime check

## Runtime Check

Imported `scripts/reproctl.py` from a non-project cwd (`/tmp`) and called
`_append_decision_log('plan','reproduce','baseline ok', phase='P1_mode_contract',
task_id='P1_T03', links='...')`. Function returned without raising; template
materialization path resolves correctly via `script_dir.parent / templates /
decision_log.md` regardless of cwd. **No code change required** — the path bug
that was discussed earlier in chat was already fixed in the on-disk file.

## Out-of-Scope Observations

- **OBS-P1-T03-1**: `_append_decision_log` creates the `.repro/repro_audit/`
  directory relative to `Path(".repro/repro_audit")` (i.e., cwd-relative), not
  the repo root. If `set_project_mode` is invoked from a cwd other than the
  project root, the audit log will be written outside the repo. Suggested fix:
  resolve `script_dir = Path(__file__).resolve().parent.parent` (the repo
  root) and put `.repro/` underneath it. **Tracked for a follow-up P1 task**;
  not blocking P1_T03 acceptance.

## Next

P1_T03 unblocks P1_T04 (Decision Log helper) and P1_T05 (commands/repro-contract.md).