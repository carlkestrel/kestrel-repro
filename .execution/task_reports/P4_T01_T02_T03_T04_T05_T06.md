# P4_T01..T06 — Training Monitor + Handoff + Memory

**Phase**: P4_monitor
**Status**: PASS

## P4_T01 — scripts/training_monitor.py
- 320 lines; import OK; dataclass Metrics with **15 fields** + Status enum with **9 values** (OK / STALLED / DIVERGED / OOM / NAN / CKPT_STUCK / CLASS_COLLAPSE / OVERHEAT / UNKNOWN)
- `classify()` priority order: NAN > OOM > OVERHEAT > DIVERGED > CLASS_COLLAPSE > CKPT_STUCK > STALLED > UNKNOWN > OK
- 5/5 classifier unit tests PASS
- `psutil` optional (graceful 0.0 fallback if missing); `nvidia-smi` graceful fallback

## P4_T02 — commands/repro-monitor.md
- 3 modes documented: `--check` (snapshot), `--watch` (continuous), `--diagnose` (interpret)
- 15 metrics + 9 statuses table + status priority
- Wraps `scripts/training_monitor.py`

## P4_T03 — templates/handoff.json
- Valid JSON Schema (draft 2020-12)
- 12 required fields: project_mode / current_phase / completed_phases / current_run_id / run_command / process_id / log_path / checkpoint_path / next_step / recovery_required_files / blockers / timestamp
- Includes worked example

## P4_T04 — commands/repro-handoff.md
- 5-step recovery flow: Locate → Schema → State consistency → Required files → Resume
- Consistency check compares handoff.json ↔ execution_state.json
- Refuses to launch if previous process still alive (PID check)

## P4_T05 — templates/project_memory.md
- §1 Project-Level Experience (5 sub-sections: Decisions / Traps / What worked / What didn't / Project facts)
- §2 Global Knowledge (5 sub-sections: Reproducibility / Data / Compute / Tooling / HITL)
- Promotion protocol §1.4 → §2 requires human + cross-project citation

## P4_T06 — TELEMETRY.jsonl + TELEMETRY_STAGES.jsonl
- Implemented in `training_monitor.py` (`_write_telemetry`, `_flush_stages`)
- Empirically verified: 2 step rows + 2 stage rows written
- `set_stage('fwd')` / `set_stage('bwd')` records stage transitions

## Files
- `scripts/training_monitor.py` (new, ~320 LOC)
- `commands/repro-monitor.md` (new)
- `commands/repro-handoff.md` (new)
- `templates/handoff.json` (new)
- `templates/project_memory.md` (new)

## Evidence
- `evidence/P4_T01_T06_monitor_test.log`