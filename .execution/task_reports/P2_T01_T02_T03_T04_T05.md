# P2_T01..T05 — Claim/Plan/Tracker templates + reproctl experiments

**Phase**: P2_claim_evidence
**Status**: PASS

## P2_T01 — claim_evidence_matrix.md
- File: `templates/claim_evidence_matrix.md`
- 11 columns in exact order: Claim / Source / Required evidence / Dataset / Protocol / Run / Artifact / Recomputed / Status / Gap / Explanation
- Status ∈ {PASS, PARTIAL, FAIL, PENDING}; Gap mandatory for non-PENDING

## P2_T02 — experiment_plan.md (M0-M10)
- File: `templates/experiment_plan.md`
- Modules M0 (data sanity) → M10 (approved extensions), all present
- 12 metadata fields per experiment: experiment_id / claim_id / module / dataset / protocol / seeds / expected_metric / compute_budget / risk_level / depends_on / evidence_paths / notes

## P2_T03 — experiment_tracker.csv
- File: `templates/experiment_tracker.csv`
- Header (exact match): `experiment_id,module,status,run_id,support_claim,parent_run_id,start_time,end_time,duration_min,gpu_hours,metric_value,status_detail`
- Zero data rows (header only)

## P2_T04 — /repro-plan command
- File: `commands/repro-plan.md`
- Describes 5-step flow: read_contract → generate_matrix → generate_plan → init_tracker → phase_switch

## P2_T05 — reproctl.py experiments API
- 3 functions added: `record_experiment()`, `update_experiment()`, `get_experiments()`
- Sub-commands: `record-experiment`, `update-experiment`, `get-experiments`
- Idempotent on `experiment_id` (no duplicate rows)
- 7/7 unit tests PASS (record / idempotency / update / filter / missing / CLI help / filter on support_claim)
- CLI smoke PASS (`record-experiment` + `update-experiment` + `get-experiments`)

## Files
- `templates/claim_evidence_matrix.md`
- `templates/experiment_plan.md`
- `templates/experiment_tracker.csv`
- `commands/repro-plan.md`
- `scripts/reproctl.py` (+~190 lines)

## Evidence
- `evidence/P2_T01..T05_matrix_plan_tracker.json`
- `evidence/P2_T05_experiments_test.log`